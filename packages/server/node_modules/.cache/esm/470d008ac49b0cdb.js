let createServer,parse,express;_c49‍.w("http",[["createServer",["createServer"],function(v){createServer=v}]]);_c49‍.w("querystring",[["parse",["parse"],function(v){parse=v}]]);_c49‍.w("express",[["default",["express"],function(v){express=v}]]);



const server = express();


server.get ('/status',(_,response)=>{
    response.send({
        status:"Okay",
    })
})

const enableCors = cors ({origin: 'http://localhost:3000'})
server
.options('/authenticate',enableCors)
.post('/authenticate',enableCors,express.json(), (request, response)=>{
    _c49‍.g.console.log(
        'E-mail', request.body.email,
        'Senha', request.body.password
    )
    response.send({
        Okay:true,
    })
})


const PORT = process.env.PORT ? parseInt(process.env.PORT):8083
const HOSTNAME = process.env.HOSTNAME || '127.0.0.1'

server.listen(PORT, HOSTNAME,()=>{
    console.log(`Server Running at http://${HOSTNAME}: ${PORT} `)
})